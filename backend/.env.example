# URL de tu nodo local (Hardhat, Ganache, etc.)
RPC_URL=http://127.0.0.1:8545

# Clave privada de la cuenta que vas a usar para desplegar
# — debe ser la clave de uno de los accounts que salen en la consola al arrancar `npx hardhat node`
PRIVATE_KEY=0xTU_CLAVE_PRIVADA_AQUI

# De momento déjalos en blanco; los completarás tras el deploy
DAO_CONTRACT_ADDRESS=
TOKEN_CONTRACT_ADDRESS=

# Puerto en el que correrá tu servidor
PORT=3000

